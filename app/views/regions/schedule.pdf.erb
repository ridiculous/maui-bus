<!DOCTYPE HTML>
<html>
<head>
  <title>Maui Bus Schedule</title>
  <style type="text/css">
      body {
          width: 100%;
      }

      .master {
          width: 100%; /* 8.5 inches */
          height: auto;
      }
      .route-wrap {
          width: 960px;
          margin: auto;
          position: relative;
      }
      .route-wrap .table {
          width: 960px;
      }
      .route-wrap table .row-header {
          padding: 4px;
          text-align: left;
          font-weight: bold;
      }

      .route-wrap table th,
      .route-wrap table td {
          padding: 4px;
          text-align: right;
      }

      .route-wrap table td.pm,
      .route-wrap table td.am {
          -webkit-box-shadow: inset 0 0px 5px 2px #FFF;
          -moz-box-shadow: inset 0 0px 5px 2px #FFF;
          box-shadow: inset 0 0px 5px 2px #FFF;
      }

      .route-wrap table td.pm {
          background: #C5D8FE;
          border: 1px solid #8CA3D0;
      }

      .route-wrap table td.am {
          background-color: #CDF3C3;
          border: 1px solid #84BE75;
      }

      .page-break {
          display: block;
          clear: both;
          page-break-after: always;
      }

      .route-name {
          font-size: 18px;
          text-align: left;
          font-weight: bold;
          margin: 10px 0 5px 0;
          padding: 6px;
          background: #EEE;
          border: 1px solid #CCC;
      }
  </style>
</head>
<body>
<div class="master">
  <div class="route-wrap">
    <div>
      <h2>Maui Bus Schedule</h2>
      <h4><b>$2 GENERAL BOARDING </b>- All Routes</h4>
    </div>
    <div style="position: absolute;right: 10px;top: 20px;">
      <table>
        <tr>
          <td class="bus-1 am">Morning</td>
          <td>&nbsp;</td>
          <td class="bus-1 pm">Evening</td>
        </tr>
      </table>
    </div>
    <% statement = Statement.new %>
    <% statement.page.items.concat(['<table class="table"><tbody>', '', '', '']) %>
    <% @regions.each do |region| %>
        <% region.routes.each do |route| %>
            <% statement = pdf_schedule_for(route, statement) %>
            <%# empty the cached object %>
            <% route.buses = nil %>
        <% end %>
    <% end %>
    <% statement.pages.each do |page| %>

        <%= page.items.join('').html_safe %>

        <div class="page-break"></div>
    <% end %>
  </div>
</div>
</body>
</html>
<%#= render('modal') %>
<%#= render('insight') %>
